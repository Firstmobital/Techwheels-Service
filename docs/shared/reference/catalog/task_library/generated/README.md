# Task Library: Generated Artifacts

Scope: holds contract-derived artifacts produced by automation only (for example, a future script that lists available contracts, validates contract structure, or compiles a contract usage report).

Subfolder purpose: none yet. Files here are never hand-edited — if a file in this folder is stale, identify its generator script, run it, and validate the output (Generated Artifact Rule, `docs/shared/reference/SYNC_PROTOCOL.md`).

Navigation: see `../INDEX.md`.

Lifecycle transition guidance: do not place hand-authored content here. If a need for hand-authored module content arises, it belongs in `../modules/` instead.

Current state: empty. No automation currently generates task-library artifacts. This anchor file exists per `docs/STRUCTURE_GUIDE.md` Section 5 item 3 (every primary category needs a README) and Section 20 (no empty category folders without an explicit anchor-file policy).
